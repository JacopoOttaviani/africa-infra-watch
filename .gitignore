# Python
__pycache__/
*.pyc

# macOS
.DS_Store

# Natural Earth download cache (~70 MB; fetch_basemap.py re-downloads as needed)
data/.ne_cache/

# Artifact builds (payload inlined), regenerated by the build scripts and only
# needed locally to publish the Claude artifacts. docs/ IS committed: GitHub
# Pages serves it straight from main.
/map.html
/dashboard.html
